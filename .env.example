# Exemplo de arquivo .env para configurações sensíveis
# Copie este arquivo para .env e preencha com seus valores
# Para gerar uma nova SECRET_KEY, execute: python generate_secret_key.py

# Django
SECRET_KEY=sua-secret-key-aqui-muito-secreta-e-aleatoria-de-50-caracteres
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Banco de Dados
# SQLite (desenvolvimento local)
# Comente as linhas abaixo para usar SQLite (padrão)

# MySQL (produção - PythonAnywhere)
DB_ENGINE=django.db.backends.mysql
DB_NAME=seuusuario$smart_donation
DB_USER=seuusuario
DB_PASSWORD=sua-senha-mysql-aqui
DB_HOST=seuusuario.mysql.pythonanywhere-services.com
DB_PORT=3306

# PostgreSQL (alternativa)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=smart_donation
# DB_USER=postgres
# DB_PASSWORD=senha
# DB_HOST=localhost
# DB_PORT=5432

# Email (para envio de notificações)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-app

# PythonAnywhere
# PYTHONANYWHERE_DOMAIN=seuusuario.pythonanywhere.com
